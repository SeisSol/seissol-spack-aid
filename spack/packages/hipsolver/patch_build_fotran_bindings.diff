diff --git a/CMakeLists.txt b/CMakeLists.txt
index da77170..72386db 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -44,9 +44,6 @@ if ( NOT DEFINED CMAKE_Fortran_COMPILER AND NOT DEFINED ENV{FC} )
 endif()
 
 project( hipsolver LANGUAGES CXX )
-if( UNIX )
-  enable_language( Fortran )
-endif( )
 
 # This finds the rocm-cmake project, and installs it if not found
 # rocm-cmake contains common cmake code for rocm projects to help setup and install
@@ -110,6 +107,12 @@ option( BUILD_VERBOSE "Output additional build information" OFF )
 # BUILD_SHARED_LIBS is a cmake built-in; we make it an explicit option such that it shows in cmake-gui
 option( BUILD_SHARED_LIBS "Build hipSOLVER as a shared library" ON )
 option(BUILD_ADDRESS_SANITIZER "Build with address sanitizer enabled" OFF)
+option(BUILD_FORTRAN_BINDINGS "Build the Fortran bindings" "${UNIX}")
+
+if(BUILD_FORTRAN_BINDINGS)
+  enable_language(Fortran)
+endif()
+
 
 add_library(hipsolver-common INTERFACE)
 target_compile_options(hipsolver-common INTERFACE
diff --git a/clients/CMakeLists.txt b/clients/CMakeLists.txt
index 170f281..9f999a5 100644
--- a/clients/CMakeLists.txt
+++ b/clients/CMakeLists.txt
@@ -36,7 +36,7 @@ endif()
 
 # This project may compile dependencies for clients
 project( hipsolver-clients LANGUAGES CXX )
-if( UNIX )
+if( BUILD_FORTRAN_BINDINGS )
   enable_language( Fortran )
 endif( )
 
@@ -56,18 +56,19 @@ if( NOT TARGET hipsolver )
   find_package( hipsolver REQUIRED CONFIG PATHS ${ROCM_PATH} /opt/rocm )
 endif( )
 
-if( UNIX )
+if( BUILD_FORTRAN_BINDINGS )
   set(hipsolver_f90_source_clients
     include/hipsolver_fortran.f90
   )
 endif( )
 
 if( BUILD_CLIENTS_TESTS OR BUILD_CLIENTS_BENCHMARKS )
-  if( UNIX )
+  if( BUILD_FORTRAN_BINDINGS )
     add_library(hipsolver_fortran_client STATIC ${hipsolver_f90_source_clients})
     add_dependencies(hipsolver_fortran_client hipsolver_fortran)
     include_directories(${CMAKE_BINARY_DIR}/include/hipsolver)
     include_directories(${CMAKE_BINARY_DIR}/include/hipsolver/internal)
+    target_compile_definitions(hipsolver_fortran_client INTERFACE HAVE_HIPSOLVER_FORTRAN_CLIENT)
   endif( )
 
   add_library(clients-common INTERFACE)
diff --git a/clients/benchmarks/CMakeLists.txt b/clients/benchmarks/CMakeLists.txt
index 408ab1e..31f33bc 100644
--- a/clients/benchmarks/CMakeLists.txt
+++ b/clients/benchmarks/CMakeLists.txt
@@ -43,7 +43,7 @@ target_link_libraries( hipsolver-bench PRIVATE
   clients-common
   roc::hipsolver
 )
-if( UNIX )
+if( BUILD_FORTRAN_BINDINGS )
   target_link_libraries( hipsolver-bench PRIVATE hipsolver_fortran_client )
 endif( )
 
diff --git a/clients/gtest/CMakeLists.txt b/clients/gtest/CMakeLists.txt
index ec627c3..6c9c089 100644
--- a/clients/gtest/CMakeLists.txt
+++ b/clients/gtest/CMakeLists.txt
@@ -57,7 +57,7 @@ set(hipsolverRf_test_source
 )
 
 add_executable( hipsolver-test ${hipsolverDn_test_source} ${hipsolverRf_test_source} )
-if( UNIX )
+if( BUILD_FORTRAN_BINDINGS )
   target_sources( hipsolver-test PRIVATE ${hipsolver_f90_source} )
 endif( )
 
@@ -87,7 +87,7 @@ target_link_libraries( hipsolver-test PRIVATE
   Threads::Threads
   roc::hipsolver
 )
-if( UNIX )
+if( BUILD_FORTRAN_BINDINGS )
   target_link_libraries( hipsolver-test PRIVATE hipsolver_fortran_client )
 endif( )
 
diff --git a/clients/include/hipsolver.hpp b/clients/include/hipsolver.hpp
index d03dae0..5ece8f8 100644
--- a/clients/include/hipsolver.hpp
+++ b/clients/include/hipsolver.hpp
@@ -24,10 +24,10 @@
 #pragma once
 
 #include "hipsolver.h"
-#ifdef _WIN32
-#include "hipsolver_no_fortran.hpp"
-#else
+#ifdef HAVE_HIPSOLVER_FORTRAN_CLIENT
 #include "hipsolver_fortran.hpp"
+#else
+#include "hipsolver_no_fortran.hpp"
 #endif
 
 // Most functions within this file exist to provide a consistent interface for our templated tests.
diff --git a/library/src/CMakeLists.txt b/library/src/CMakeLists.txt
index 4bae75a..3dfd425 100644
--- a/library/src/CMakeLists.txt
+++ b/library/src/CMakeLists.txt
@@ -62,7 +62,7 @@ set (hipsolver_f90_source
   hipsolver_module.f90
 )
 
-if( UNIX )
+if( BUILD_FORTRAN_BINDINGS )
   # Create hipSOLVER Fortran module
   add_library(hipsolver_fortran ${hipsolver_f90_source})
 endif( )
@@ -195,7 +195,7 @@ else( )
   )
 endif( )
 
-if( UNIX )
+if( BUILD_FORTRAN_BINDINGS )
   # Force installation of .f90 module files
   install(FILES "hipsolver_module.f90"
           DESTINATION "${CMAKE_INSTALL_INCLUDEDIR}/hipsolver")

